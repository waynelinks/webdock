version: "2.4"
services:
  web_tester:
    volumes:
      - type: volume
        source: npm_cache
        target: /root/.npm
      - type: bind
        source: ../web-client-tester/app
        target: /app

volumes:
  npm_cache:
    driver: local
    name: global_npm_cache
