version: "2"
services:
  web:
    volumes:
      - ./app/public/:/app/public/

  assets_builder:
    image: damlys/webdock-web-assets-builder:example
    volumes:
      - npm_cache:/var/cache/npm
      - ./app/:/app/
    working_dir: /app/

volumes:
  npm_cache:
