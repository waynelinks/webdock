#!/usr/bin/env groovy

node {
    checkout scm

    def dockerImage

    stage('Build') {
        dockerImage = docker.build('damlys/webdock-web-client')
    }

    stage('Run') {
        echo 'TODO'
    }

    stage('Test') {
        echo 'TODO'
    }

    stage('Release') {
        docker.withRegistry('https://registry.hub.docker.com', 'docker-hub-credentials') {
            dockerImage.push('example')
        }
    }

    stage('Deploy') {
        echo 'Deploying image...'
    }
}
