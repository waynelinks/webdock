version: "3.7"
services:
  this:
    build:
      context: .
      dockerfile: Dockerfile
    image: damlys/webdock-web-client-foundation:${VERSION-unreleased}
    command: ["bash"]
    networks:
      - host

networks:
  host:
    external: true
