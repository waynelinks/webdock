version: "3.7"
services:
  this:
    build:
      context: .
      dockerfile: Dockerfile
    image: damlys/webdock-fluent-collector:${VERSION-unreleased}
    command: ["ash"]
    networks:
      - host

networks:
  host:
    external: true
