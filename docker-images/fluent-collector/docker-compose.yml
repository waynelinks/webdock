version: "3.7"
services:
  this:
    build:
      context: .
      dockerfile: Dockerfile
    image: damlys/webdock-fluent-collector:${VERSION-unreleased}
    command: ["ash"]
    networks:
      - none

networks:
  none:
    external: true
