version: "3.7"
services:
  this:
    build:
      context: .
      dockerfile: Dockerfile
    image: damlys/webdock-fluent-forwarder:${VERSION-unreleased}
    command: ["bash"]
    networks:
      - none

networks:
  none:
    external: true
