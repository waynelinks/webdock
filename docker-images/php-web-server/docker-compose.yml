version: "3.7"
services:
  this:
    build:
      context: .
      dockerfile: Dockerfile
    image: damlys/webdock-php-web-server:${VERSION-unreleased}
    command: ["bash"]
    volumes:
      - type: volume
        source: global_composer_cache
        target: /var/cache/composer
#      - type: bind
#        source: app
#        target: /opt/app

volumes:
  global_composer_cache:
    external: true
